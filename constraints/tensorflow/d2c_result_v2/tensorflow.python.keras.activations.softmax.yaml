constraints:
  axis:
    default: '-1'
    descp: Integer, axis along which the softmax normalization is applied.
    dtype:
    - int
  x:
    descp: Input tensor.
    dtype:
      - tf.tensor
inputs:
  optional:
  - axis
  required:
  - x